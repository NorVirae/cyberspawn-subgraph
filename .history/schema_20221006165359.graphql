type Notification @entity(immutable: true) {
  id: ID!
  from: User! @derivedFrom(field: )
  to: String!
  message: String!
  
}

type User @entity(immutable: true){
  id: ID!
  notifications: [Notification]
}